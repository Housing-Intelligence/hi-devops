name: Databricks Deploy

on:
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Databricks deploy script
        run: |
          chmod +x scripts/databricks/deploy.sh || true
          scripts/databricks/deploy.sh
